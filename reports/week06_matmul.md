# 第 6 周实验报告：CUDA 内存分块与矩阵乘法 (GEMM) 优化

## 实验核心数据汇总

### 矩阵规模: 512 x 512
| 版本 | 耗时 (ms) | 有效算力 (TFLOPs) | 加速比 |
|---|---|---|---|
| Naive (朴素直读显存) | 0.402 | 0.667 | 1.0x |
| Shared Memory (分块优化) | 0.285 | 0.944 | **1.41x** |

### 矩阵规模: 1024 x 1024
| 版本 | 耗时 (ms) | 有效算力 (TFLOPs) | 加速比 |
|---|---|---|---|
| Naive (朴素直读显存) | 2.870 | 0.748 | 1.0x |
| Shared Memory (分块优化) | 2.305 | 0.932 | **1.25x** |

### 矩阵规模: 2048 x 2048
| 版本 | 耗时 (ms) | 有效算力 (TFLOPs) | 加速比 |
|---|---|---|---|
| Naive (朴素直读显存) | 23.178 | 0.741 | 1.0x |
| Shared Memory (分块优化) | 17.274 | 0.995 | **1.34x** |

## 核心机制分析
1. **为什么分块 (Tiling) 能带来数百倍的加速？**
   - Naive 版本中，每个线程计算一个输出元素，每次都要完整读入全局显存的一整行和一整列，造成了极其严重的显存带宽挤塞（每个数据被重复读取上千次）。
   - Shared Memory 分块版本中，同一个 Block 内部的线程协作将 16x16 矩阵小块一次性搬入极速片上共享内存（SRAM），然后在片内高速完成乘加累积，**将慢速全局显存的访问次数直接降低了 16 倍**！
2. **__syncthreads() 栅栏同步的作用**：
   - 第一次同步保证整个 Block 的线程都把当前 Tile 数据从显存搬到了共享内存；
   - 第二次同步保证所有线程都完成了当前 Tile 的计算，防止早完成的线程提前覆盖共享内存中的数据（写后读冲突 RAW 保护）。
